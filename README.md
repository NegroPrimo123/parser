Высоконагруженный сервис для параллельного парсинга вакансий с hh.ru с использованием worker pool, rate limiting и экспортом в PostgreSQL/CSV.

Ключевые технологии: Go, concurrency (worker pool), rate limiting (token bucket), retry with backoff, PostgreSQL, Prometheus metrics, Docker.
